@using MatBlazor
@page "/register"


<PageTitle>Login</PageTitle>

<h1>Login</h1>

<p>Please provide the following information, and we will get back to you about your user.</p>

<p>
    <MatStringField @bind-Value="@FirstName" Label="First Name"></MatStringField>
</p>
<p>
    <MatStringField @bind-Value="@LastName" Label="Last Name"></MatStringField>
</p>
<p>
    <MatStringField @bind-Value="@Email" Label="E-mail address"></MatStringField>
</p>
<p>
    <MatStringField @bind-Value="@Affiliation" Label="Place of work"></MatStringField>
</p>
<p>
    <MatStringField @bind-Value="@JobTitle" Label="Jobtitle"></MatStringField>
</p>

<MatButton OnClick="@Click" Raised="true">Sign up</MatButton>
@code{
    string FirstName;
    string LastName;
    string Email;
    string Affiliation;
    string JobTitle;


    string ButtonState = "";
    void Click(MouseEventArgs e)
    {
        ButtonState = "Clicked";
    }
        
    /* Call some service that calls a repo, or just call a repo?
    MedicalProfessionalUserModel MPUser = new MedicalProfessionalUserModel{
        Id = 0;//get data somehow, autoincrement
        FirstName = FirstName;
        LastName = LastName;
        Email = Email;
        Affiliation = Affiliation;
        JobTitle = JobTitle;
        TotalAssignmentsFinished = 0;
        ProfilePictureID = 0; //this should probably not be an ID but instead be a direct image upload.
    }
   */
}